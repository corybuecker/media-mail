<%= render partial: 'form' %>

<%= turbo_frame_tag "links" do %>
  <% links.each do |link| %>
    <%= render partial: 'link', locals: { link: link, page: link.page } %>
  <% end %>
<% end %>

<%= turbo_stream_from :links %>
